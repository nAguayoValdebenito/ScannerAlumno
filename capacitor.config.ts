import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'nAguayoValdebenito.scannerAsistencia',
  appName: 'ScannerAsistencia',
  webDir: 'www'
};

export default config;
